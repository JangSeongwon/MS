ftp: Servo

Libreoffice
tar -xvf 

(   tar : 파일을 압축하거나 압축을 해제할 때 사용한다.
     -c : 파일을 tar로 묶는다.
     -f : 압축할 파일을 지정한다.
      -x : 압축을 해제한다.
      -z : gzip으로 압축한다.
       -t : tar로 압축된 내용을 확인한다.
      -p : 파일 권한을 저장한다.
      -v : 처리 과정을 자세히 보여준다.
      -C : 경로를 지정한다.
      )

cd DEBS
sudo dpkg -i *.deb

Chrome

ROS Setup

